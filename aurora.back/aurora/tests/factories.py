import factory
import pytest
from faker import Faker
from pytest_factoryboy import register

fake = Faker()
from aurora.blockchain.models import Transaction


class TransactionFactory(factory.django.DjangoModelFactory):
    class Meta:
        model = Transaction

    nonce = factory.Sequence(lambda n: str(n))
    from_address = fake.lexify(text="187?????????????????????????????????????????????????????????????")
    to_address = fake.lexify(text="361?????????????????????????????????????????????????????????????")
    amount = 20
    fee = 5


register(TransactionFactory)
